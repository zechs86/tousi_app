# 再開メモ（次のセッションはここから）

最終更新: 2026-06-24

## いまの状態（できたこと）
- ダッシュボード（ブラウザ画面・スマホ対応）完成: `src/dashboard.py` ＋ `dashboard.bat`
  - 3タブ: 🔎今ここ！(スキャナー) / 📊銘柄分析(チャート＋ファンダ) / 📰ニュース
- 全銘柄スキャナー完成・動作確認OK: `src/scanner.py`（108銘柄→押し目/急騰ブレイクを検出）
- スマホ通知(ntfy)動作確認OK: `src/notify.py`（トピック tousi-knak-r7m3q9）
- streamlit / plotly インストール済み
- 設定: `src/config.py`（損切り5%/利確10%、SCAN_TOP_N=8）

## ローカルで今すぐ見る方法
- `dashboard.bat` をダブルクリック → ブラウザでダッシュボードが開く
  - 初回「Welcome to Streamlit / Email」が出たら、空のままEnter（メール不要）
- ★前回ダッシュボードは正常起動・操作確認できた（ログでチャート描画を確認）。
- スマホ（同じWiFi）から見る: ブラウザで http://192.168.0.171:8501 （PCのIP。変わる場合あり）
- 軽微な直し: dashboard.py の `use_container_width=True` は将来非推奨 → `width='stretch'` に置換する（動作はする）。

## クラウド化（PC不要にする）の進捗
ゴール: PCの電源を切っても、スマホでどこでも使える状態。
1. ✅ git有り(2.54.0)。gh CLI(2.95.0)を winget で導入・`gh auth login`済み(zechs86)。
2. ✅ GitHubへpush完了。**private**リポジトリ: https://github.com/zechs86/tousi_app
   - 追加済みファイル: requirements.txt(rich含む), .gitignore(.venvとmarket.db除外)
3. ⬜ **Streamlit Cloud に接続してダッシュボードを公開（次はここ）**。
   - https://share.streamlit.io → GitHubでサインイン → New app
   - Repo: zechs86/tousi_app / Branch: main / Main file: src/dashboard.py
   - private repoなのでStreamlitにアクセス許可(Authorize)が要る。
   - 公開後のアプリURLを友人にシェアすればOK(リポジトリは見せずに済む)。
4. ✅ GitHub Actions(cron)で朝夜の通知を自動化。手動実行(workflow_dispatch)で成功確認済み。
   - `.github/workflows/notify.yml`（朝8:15/夜18:30 JST）/ `notify_job.py`（scan→ntfy）
   - gh push時、workflowファイルには `workflow` スコープ追加(`gh auth refresh -s workflow`)が必要だった。
   - 通知トピックを秘密化したい時は repo Settings>Secrets に NTFY_TOPIC を登録。

## 未対応の依頼
- ダッシュボードの用語・マークを「タップで説明が出る」ようにする（ツールチップ help= ＋ 用語ガイドの追加）。

## 注意
- 前セッションはツール操作の不具合で画面が乱れた。新セッションで継続すればOK。
- 詳しい全体像は同フォルダの README.md を参照。
