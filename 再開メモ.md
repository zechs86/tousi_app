# 再開メモ（次のセッションはここから）

最終更新: 2026-07-01

## アプリの全体像（クラウド稼働中・スマホ対応）
ダッシュボード `src/dashboard.py`（ネオン調）。**ページ移動方式**（タブではなくラジオをピル風CSS）。
サイドバーに **👤利用者**（名前。共有時に各自を分ける。空欄=ゲスト。一度入れれば保持・URL ?u= にも記憶）。

ページ:
- 🔎 今ここ！ … 全銘柄スキャナー(`scanner.py`)。各カードに「📝ペーパーで買う準備」ボタン→💰ペーパーへ移動し利確/損切りを自動prefill
- 📊 銘柄分析 … チャート＋ファンダ(PER/PBR/配当/ROE/自己資本比率/営業利益率/配当性向)＋📑財務5期推移(`fundamentals.py`)。上部に **⭐お気に入り**（ワンタップ表示）＋☆登録/⭐解除（`favorites.py`）
- 🤖 AI分析 … Claudeが1銘柄分析(業績/機関投資家/目標株価/なぜ今か)。`ai_analysis.py`。手動1回約¥4
- 💬 AI相談 … **既定オフ**(`config.AI_CHAT_ENABLED=False`・従量課金のため)。コードは`ai_chat.py`に有り
- 💰 ペーパー … 仮想売買・資産推移グラフ・勝率・利確🎯/損切り🛑(購入後も確認/変更可)。`paper.py`
- 📰 ニュース … GoogleニュースRSS＋簡易センチメント

裏方: `risk.py`(暴落検知) / `notify_job.py`(朝夜通知=買い+⚠️リスク+🤖AIコメント+📱アプリURL) /
`store.py`(保存抽象化) / `indicators.py` `signals.py` `universe.py`(約110銘柄) / `config.py` / `secret_local.py`(鍵・gitignore)

## データ永続化（DB）= 設定済み（重要）
- `src/store.py` が **Upstash Redis(無料・REST)** に保存。未設定ならdata/kv/にファイル。
- 認証: `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN`。
  - 手元: `src/secret_local.py` に記入済み。
  - クラウド: **Streamlit Secrets に登録済**（ユーザーが貼付・Reboot済）。
  - GitHub Actions: **Secrets登録済**（`gh secret set --body`）。
- キー: ペーパー=`paper:<user>` / お気に入り=`fav:<user>`。→ 人ごと・クラウドでも消えない。
- Upstash URL例: https://intense-bat-136130.upstash.io （アカウントはGitHub:zechs86でログイン）

## コスト方針（ユーザーは従量課金に敏感）
- 自動課金は **朝夜通知のAIコメントのみ**（`config.AI_NOTIFY_COMMENT=True`・月¥20〜40）。
- 手動の🤖AI分析=押した時だけ¥4。AI相談=オフ。
- AI_MODEL=`claude-sonnet-4-6`(既定)。最高精度は`claude-opus-4-8`。
- console.claude.com の Usage limits 設定を案内済。AI機能を足す時は必ず既定オフ＆コスト明示。

## 開発フロー / ハマりどころ
- 変更→`git push origin main`でStreamlit自動再デプロイ。GitHub: **public** `zechs86/tousi_app`。
- push前に `gh auth login`(トークン切れがち)。workflow変更時は `gh auth refresh -s workflow`。
- gh: `C:\Program Files\GitHub CLI\gh.exe`。デバイスログインはバックグラウンド実行で8桁コードを読む方式。
- GitHub Secret登録は PowerShellでは `gh secret set NAME --body $val`(パイプはstdin文字化け)。
- ntfy: Titleは英数字のみ／本文~4096バイト(1100字で打切)／トピックはstrip。
- 株価dfは `dropna(subset=['Close'])`。get_price/last_priceはtry/exceptで保護(yfinance一時エラー対策)。
- アプリURL: https://tousiapp-halx5hjmpkl8fkzcyq2gqn.streamlit.app

## 次の候補（ユーザーは「全部やる」意向）
1. ~~利確/損切り到達アラート~~ ← **✅完了(2026-06-26)**。`notify_job.build_paper_alerts()`が
   `paper.all_users()`(=`store.list_keys('paper:*')`)で全利用者を走査、保有×targets/stopsを
   `_fetch_prices`(yfinance直近終値)と比較。到達したら **最優先(priority=high・🚨)で別ntfy通知**。
   重複連打しないよう各stateの`alerts_sent`に記録し**1回だけ**通知、条件解除で自動再アーム。
   朝夜の通常スキャン通知より前に送る。検証: 一時ユーザーで利確/損切り両方発火＆2回目0件を確認済。
2. AI相談に保有を反映（動的システムプロンプト・get_my_portfolioツールは準備済み）※オン時のみ
3. ペーパー成績の更なる可視化、決算カレンダー 等

**✅追加(2026-06-26): 株主優待カウントダウン**を朝夜通知に表示。`notify_job.build_yutai_reminder()`が
`watch.RECORD_MONTHS`(イオン8267=2月/8月)の権利付最終日まで残り日数を計算し、`config.YUTAI_REMIND_DAYS`
(既定75)以下なら「🎁あとN日＋現在値＋1年レンジ位置(安値圏/中間/高値圏)」を通知。無料(価格取得のみ)。
0以下で停止。→ イオン優待を“安く仕込む”目標に直結(現在62日前・¥1300・1年レンジ0%=安値圏)。

**✅追加(2026-06-26)第2陣・4機能（全テスト済）:**
1. **接近アラート**: 到達前(config.NEAR_ALERT_PCT=2%以内)で「もうすぐ利確/損切り(あとX%)」を予告→到達で「到達！」。
   `alerts_sent`に段階(None/near/reached)を記録しランク前進時だけ通知(後退は無音)。`_level_status()`。
2. **優待を複数銘柄対応**: `config.YUTAI_RECORD_MONTHS`(dict)を`watch.RECORD_MONTHS`に合算。ここに足すだけで優待銘柄追加。
   ※月は要確認(優待は変更/廃止あり)。build_yutai_reminderは月マップから権利付最終日を直接計算。
3. **決算カレンダー**: `build_earnings_reminder()`がお気に入り＋保有の和集合(`_watched_codes()`・上限20)の
   次回決算日(yfinance Ticker.calendar)を取得、`config.EARNINGS_REMIND_DAYS`(14)以内を「📅あとN日」で通知。
   ※日本株は決算日が無い/ズレることあり=取れた範囲。無料。
4. **ペーパー成績の可視化**: `paper.monthly_realized()`/`paper.by_symbol()`を追加、ダッシュボードの💰ペーパーに
   「📈成績の内訳」expander(月別実現損益の棒グラフ＋銘柄別 損益/勝率カード)を表示。
- 新configキー: NEAR_ALERT_PCT / YUTAI_RECORD_MONTHS / EARNINGS_REMIND_DAYS（src/config.pyに集約・各0でオフ可）。

**✅追加(2026-06-26)第3陣: 🗓️予定タブ**
- 新モジュール `src/calendar_view.py`(優待/決算の予定を作る共通部品): `yutai_schedule()`(優待カレンダー=
  残日数+現在値+1年レンジ位置)・`earnings_schedule(codes)`(yfinance Ticker.calendarで次回決算日)・`price_range()`。
- notify_jobの`build_yutai_reminder`/`build_earnings_reminder`を**calendar_viewを使う形にリファクタ**(出力は不変・重複解消)。
- ダッシュボードに**「🗓️ 予定」ページ追加**(PAGESに挿入): 🎁優待カレンダー(全優待銘柄)＋📅決算カレンダー
  (お気に入り＋保有の和集合)をカード表示。残日数が近いと色付き。AppTestで実Streamlit動作確認済。
- 注: 予定ページ内で `import paper` 必須(ページ単位importのため)。新規ページ追加時は各ページ先頭でimportする流儀。

**✅追加(2026-06-26)第4陣: 押し目買いアラート**
- `notify_job.build_dip_alerts()`: お気に入り銘柄が「上昇トレンド(SMA25>SMA75)中にRSI≤config.DIP_RSI(40)」=
  改良③の押し目買いゾーンに入ったら朝夜通知に「🟢押し目買いゾーン(上昇中・RSIxx)」表示。`_dip_signal()`が
  indicators.add_all_indicatorsで判定。利用者ごとに`store`キー`dipsent:<user>`で1回だけ通知・ゾーン離脱で再アーム。
- config: `DIP_ALERT_ENABLED`(True)/`DIP_RSI`(40)。無料(価格取得のみ)。Falseでオフ。
- 注: notify_jobに`import store`追加(build_dip_alertsが直接store使用。_watched_codesはtry内だったので今まで露見せず)。
- 検証: ソニー6758(上昇中RSI39)で点灯→2回目無音を確認。イオン8267は下降中なので点灯せず(正しい)。
- ユーザーが最初に望んだ「常に情報集めて買いどころ通知」の核心機能。お気に入りに入れた銘柄を自動見張り。

**✅追加(2026-06-26)第5陣: 利回り表示(配当＋優待＝総合)**
- `calendar_view.dividend_yield_pct(code,price)`(deepdiveと同じ堅牢ロジック: dividendRate÷株価優先)を追加。
- `yutai_schedule()`の各行に div_yield/yutai_value/yutai_yield/total_yield/unit_cost(100株必要額)を付与。
  優待価値は`config.YUTAI_VALUE_YEN`(コード:年間円。任意)から。優待利回り=優待価値÷(株価×100)。
- 🗓️予定の優待カードに「利回り 配当X% ／ 優待Y% ／ 総合Z%（100株¥…）」を表示(取れたものだけ)。
- イオンの優待はキャッシュバック型で固定額でない→YUTAI_VALUE_YENは既定空(配当のみ表示)。年間利用額×3%目安で各自設定。
- 検証: イオン配当1.08%/100株¥130,150。優待値6000円設定時 優待4.61%・総合5.69%。予定ページAppTest OK。

**✅追加(2026-06-26)第6陣: お気に入りウォッチ(🔎今ここ!ページ最上部)**
- アプリを開いた最初の画面(🔎今ここ!)の冒頭に、お気に入り銘柄の今の状態を一覧表示。
- 各銘柄: トレンド状態(🟢押し目買いゾーン/↗上昇/↘下降=待ち)＋現在値＋RSI＋1年レンジ位置(安値圏/中間/高値圏)。
- 実装: `get_price`(生OHLC)に`add_all_indicators`を付与しSMA25/75・RSIから判定(改良③と同基準)。
  len<80(SMA75不足)はスキップ。favorites.load(USER)で利用者ごと。AppTestで描画確認済(イオン表示OK)。
- 「常に情報を見える化して買いどころを知る」目標の、通知に続くアプリ内ハブ。

**✅追加(2026-06-26)第7陣: ニュースAI要約(既定オフ・押した時だけ課金)**
- `ai_analysis.summarize_news(name,items)`: 見出しを初心者向けに数行要約(全体の雰囲気/注目ポイント/注意点)。
- `config.AI_NEWS_SUMMARY_ENABLED=False`(既定オフ)。Trueかつキー有りの時だけ📰ニュースに要約ボタン表示、
  押した時のみ約¥1〜2課金。コスト方針(AI系は既定オフ＆明示)に準拠。
- 検証: 無課金のガード経路(キー無し/空ニュース)のみ確認。実API呼び出しは課金回避のため未実施
  (既存comment_on_scanと同一パターンなので動作確度高)。試すなら config の flag を True にして1回押す。

**✅追加(2026-06-26)第8陣: 優待価値の入力UI(アプリ上で総合利回り)**
- 🗓️予定に「🎁優待の価値を入れて総合利回りを見る」expander。優待の年間価値(円)を入力→保存で総合利回り反映。
- 保存は利用者ごと `store` キー `yutaival:<user>`。`calendar_view.yutai_schedule(yutai_values=...)`で上書き(configより優先)。
- イオンはキャッシュバック型なので「年間利用額×3%が目安(例 年20万→6000円)」とガイド。プログラム不要でOK。
- 検証: 6000円入力で優待4.59%・総合5.66%。予定ページAppTest(number_input描画)OK。

**✅追加(2026-06-26)第9陣: ⚙️設定ページ(コード不要で全設定調整)**
- 新モジュール `src/appconfig.py`: 上書き可能な設定(FIELDS)をstoreキー`appcfg`(アプリ共通)に保存し、
  `apply_to_config()`でconfigへ反映。`_DEFAULTS`に起動時の真の既定を控え、リセットも効くように。
- 反映箇所: dashboard.py 起動直後 と notify_job.main()冒頭(両プロセスで効く=朝夜通知にも反映)。
- 対象設定: DIP_RSI/DIP_ALERT_ENABLED/YUTAI_REMIND_DAYS/EARNINGS_REMIND_DAYS/NEAR_ALERT_PCT/
  SCAN_TOP_N/AI_NOTIFY_COMMENT/AI_NEWS_SUMMARY_ENABLED。
- ダッシュボードに「⚙️ 設定」ページ追加(トグル/数値入力＋保存/既定に戻す)。AI系はオンで課金と明示。
- 検証: save→apply でconfig上書き、空保存でリセット(DIP_RSI 40→25→40)。設定ページAppTest(3トグル/5数値)OK。
- 注意: configをモジュール属性として書き換える方式。getattr(config,X)で読む箇所に効く。Streamlitは同一プロセス
  再利用なので_DEFAULTSスナップショットが無いとリセット不能だった(修正済)。

**✅追加(2026-06-26)第10陣: 分割買いシミュレーター＋ウォッチバッジ＋通知ヘッダー**
1. 🗓️予定に「🧮 分割買いシミュレーター」expander。目標株数/回数/開始価格/下げ幅%→各回の価格・株数・必要額・
   平均取得単価・一括との差を試算。開始価格は優待銘柄の現在値を既定。検証: 100株3回5%刻みで平均¥1,236・差¥6,435。
2. 🔎今ここ!のお気に入りウォッチに優待🎁/決算📅の近接バッジ。calendar_view.yutai_schedule(with_price=False・高速)＋
   earnings_schedule(favs)で日数取得、YUTAI_REMIND_DAYS/EARNINGS_REMIND_DAYS以内のみ表示。
3. 朝夜通知の先頭に一目サマリー行(🟢買いN・押し目N・🎁優待N日・📅決算N・⚠️リスクN)。
- 全てAppTest/単体テスト済。実機未確認は変わらず。

**✅追加(2026-06-26)第11陣: 適時開示(TDnet・公式開示)**
- 新モジュール `src/tdnet.py`: yanoshinの無料TDnet WebAPI(認証不要)から適時開示を取得。
  `fetch_disclosures(code,limit)`→[{id,date,title,url,company,important}]。IMPORTANTキーワード(決算/業績修正/
  配当/優待/自己株式/TOB等)で注目度判定。失敗時は[](画面壊さない)。
- 📰ニュースページに「📑適時開示(TDnet)」セクション(⭐=注目・タイトルはPDFへリンク)。Googleニュースより高精度の“本物の材料”。
- 通知: `notify_job.build_disclosure_alerts()`がお気に入り＋保有(_watched_codes・上限15)の注目開示の**新着**を通知。
  storeキー`tdnetseen`(直近500id)で重複防止。`config.TDNET_ALERT_ENABLED`(既定True・無料)。通知の並びは優待→📑開示→押し目→決算→買い→リスク→AI。ヘッダーに📑開示N。
- 検証: イオンの新株予約権開示2件を通知→2回目0件(dedup)。ニュースページAppTestで開示描画OK。
- ユーザーの方針「機関投資家動向・確かな情報源重視」とロードマップ②決算/TDnet要約に合致。

**✅追加(2026-06-26)第12陣: 暴落リスク検知の強化＋ウォッチに最新開示**
1. `risk.py`に検知2種追加: **約3ヶ月の安値更新**(close≤期間最安値・下落時)と**寄りギャップダウン**(GAP_DOWN=-3%)。
   severityは下げ幅/ギャップ/新安値(10点)の最大で並べ替え。検証: 40銘柄で新安値(日産/楽天)・ギャップ(東エレク)検出。
2. 🔎今ここ!のお気に入りウォッチに**最新の適時開示1件**を表示(tdnet.fetch_disclosures limit=1・⭐注目/📑・PDFリンク)。
- 検証: risk.py試走OK、今ここページAppTestで開示行描画OK。
- リスク管理重視(大ケガしない)の方針に合致。

**✅追加(2026-06-26)第13陣: リスク管理「買ってよい株数の目安」(📊銘柄分析)**
- 📊分析に「🛡️リスク管理」expander。1回で失ってよい上限(既定config.RISK_PER_TRADE_YEN=5000)＋損切り幅%から
  適正株数を逆算(株数=上限÷(株価×損切%)・単元100株切捨)。必要資金/損切り価格/想定最大損失を表示。
- 20日日次標準偏差からボラティリティ(±X%)を出し、損切り幅は値動き2回分以上を推奨(sugg_stop=max(STOP_LOSS_PCT,2σ))。
- 単元に届かない予算なら警告＋単元未満株(楽天かぶミニ/S株)での株数を提示。
- 検証: 株価1300・損切3%で100株/最大損失¥3,900。5%＋予算5000では単元未達→S株76株提案(数学的に正)。分析ページAppTest OK。
- 「損切りを守り大ケガしない」方針の実用ツール。

**✅追加(2026-06-26)第14陣: 株数計算をペーパー購入に連携・共通化**
- `risk.suggest_shares(price,stop_pct,budget,is_jp)`を新設(株数逆算をDRY化)。📊分析の🛡️計算もこれを使う形にリファクタ。
- 💰ペーパー購入フォームに「🛡️目安 N株(損切りX%・上限¥…で最大損失¥…)」＋「この株数にする」ボタン(paper_buy_shに反映)。
  単元未達時は「リスク大」と警告。既定はconfig.RISK_PER_TRADE_YEN/STOP_LOSS_PCT。
- 検証: suggest_shares(300円/5%/5000)=300株、両ページAppTest OK。買う前に適正株数が分かる導線。

**✅追加(2026-06-26)第15陣: 「今ここ!」候補カードに株数目安**
- スキャナー各候補に🛡️株数目安を表示。候補自身の損切り価格から損切り%を出し`risk.suggest_shares`で逆算
  (上限config.RISK_PER_TRADE_YEN)。単元未達は「リスク大」表示。必要資金も併記。今ここAppTest OK。
- 分析/ペーパー/今ここの3画面で株数目安が一貫(全てrisk.suggest_shares)。

**✅追加(2026-06-26)第16陣: 朝夜通知の買い候補にも株数目安**
- `notify_job.build_message`が各候補の損切り価格から`risk.suggest_shares`で株数を出し「🛡️N株」を2行目に付与。
  単元未達(予算不足)時は非表示。検証: 300円株→🛡️300株、上限2万なら1250円株→🛡️200株。
- これで通知だけで候補×株数まで分かる。アプリの3画面＋通知の計4箇所で株数目安が一貫。

**✅追加(2026-06-26)第17陣: 適時開示のAI要約(ロードマップ②完成)**
- `ai_analysis.summarize_disclosures(name,discs)`: TDnet開示一覧をAIが初心者向け解説(一番重要な開示/株価への影響/
  用語かみくだき)。📰のTDnetセクションに「🤖適時開示をAIで要約」ボタン。`config.AI_NEWS_SUMMARY_ENABLED`(既定オフ)
  かつキー有りの時だけ表示・押した時のみ¥1〜2。ニュース要約と同じトグルで管理。
- 検証: ガード経路(キー無し/空)・ニュースページAppTest OK。実API未呼出(課金回避)。
- ロードマップ②「決算/TDnet要約」を達成。全機能の回帰テスト(全7ページ＋全モジュール)もパス済(第16陣後)。

**✅追加(2026-06-26)第18陣: ④価格予測を“安全な統計”として実装(過去の似た局面)**
- 新モジュール `src/analog.py`: `historical_analog(dfi,horizon)`=今日と似た局面(同トレンド＋RSI±7)の過去日を集め
  horizon営業日後の変化率を集計(勝率/平均/中央値/最大/最小)。サンプル<10はNone。**予測ではなく傾向の参考**と明示。
- 📊銘柄分析に「📊過去の似た局面」expander(5日後/20日後)。サンプル用に`get_price_long`(5年・キャッシュ)を追加。
- **意図的に“当てる予測”にしない**: 初心者が損切り規律を崩さないよう、確率と下振れ(最小値)を見せる設計。
  検証: イオン下降局面で5日後上昇率50%/平均-0.5%/最小-10.8%=「下降中は優位性なし・下振れ大」を正しく表示。分析ページAppTest OK。
- これでロードマップ①〜④が(④は安全版で)出揃った。

**🐛重大修正(2026-06-26)第19陣: Actionsの朝夜ジョブにUpstash認証が渡っていなかった**
- `notify.yml`のnotifyステップenvに`UPSTASH_REDIS_REST_URL/TOKEN`が無く、本番の朝夜ジョブでは`store`が
  ファイルfallback(Actionsは毎回まっさら)になっていた。＝**利確到達アラート・押し目アラート・開示dedup・
  ⚙️設定の反映・favorites由来の決算/開示が、実ジョブでは機能していなかった**(ローカル/手元は secret_local で動作)。
- 修正: `notify.yml`のenvに2行追加(Secretsは登録済みだったが渡されていなかっただけ)。
- 影響: 第4弾の“本命”利確到達アラートも実ジョブ初稼働はこの修正後から。手動実行(workflow_dispatch)で要確認。
- 注: workflowファイルのpushは`workflow`スコープ必要(`gh auth refresh -h github.com -s workflow`)。
- **本番検証済**: 修正後に手動実行(gh workflow run notify.yml)→成功(1分・通知送信成功・エラーなし・env に UPSTASH_* が *** で渡る)。

**✅追加(2026-06-26)第20陣: ⚙️設定に接続状態＆テスト通知(自己診断)**
- ⚙️設定に「🔌接続状態」カード: 保存先(Upstash接続/ファイル)・通知トピック有無・AIキー有無を表示(store.using_db等)。
- 「📲テスト通知を送る」ボタン(トピック有時のみ)。アプリから直接ntfy送信して導通確認。
- `config._load_ntfy_topic`がStreamlit Secretsも読むよう修正(クラウドのアプリからテスト送信できるように。
  従来は環境変数→secret_localのみでst.secrets未対応だった)。クラウドのSecretsに NTFY_TOPIC を入れれば有効。
- 検証: 設定ページAppTest(接続カード描画・ボタン3つ)OK。NTFY_TOPIC/using_db 取得OK。

**✅追加(2026-06-27)第21陣: 📊分析のファンダに時価総額＋成長率**
- 既存のファンダ欄(PER/PBR/配当利回り)に2段目: 時価総額(兆/億円整形)・売上成長(revenueGrowth)・利益成長(earningsGrowth)。
  成長率は+で緑/-で赤。ユーザー重視の「個別成長株」視点を強化。
- 検証: イオン=時価総額3.61兆円/売上+11.3%/利益+75.4%。分析ページAppTest OK。

**✅追加(2026-06-27)第22陣: 自動テスト(回帰防止)＋CI**
- `tests/test_logic.py`: 純粋ロジックの回帰テスト(pytest不要・標準assertのみ・ネット接続なし)。36項目。
  対象: risk.suggest_shares / analog.historical_analog / paper.monthly_realized・by_symbol・stats /
  notify_job._level_status / tdnet._code_for_api・is_important / appconfig._coerce・effective / watch営業日ロジック。
  実行: `.venv\Scripts\python.exe tests\test_logic.py`(全OKでexit0)。notify_jobはルート直下なのでsys.pathにroot追加。
- `.github/workflows/test.yml`: push/PRで上記テストを自動実行(requirements.txt導入後)。壊れたら即分かる。
- 36 OK / 0 NG を確認。今後の変更はこのテストが守る。

**✅(2026-06-27)第23陣: エージェント監査→バグ修正＋予想精度の実証的改善**
- 2エージェント(バグ監査・バックテスト)を走らせ、結果を自分で独立検証して反映。
- **バグ修正(6件)**:
  ① notify_job: `scan()`をtry/exceptで保護(Yahoo一時障害でブリーフィング全損を防ぐ)。
  ② `_watched_codes`を`sorted()`に(set→list[:N]は順序不定で特定銘柄が恒常的に監視外になり得た)。
  ③ build_disclosure_alertsのseen dedupを挿入順保持に(set化で取りこぼし→再通知の恐れ)。
  ④ paper.py に `import json` 追加(直接実行時のNameError)。
  ⑤ calendar_view.dividend_yield_pct: dividendRate無時のdividendYield推測(×100)を廃止→Noneに(0.7→70%誤表示回避)。
  ⑥ scanner.scan/risk.detect_risks: 単一銘柄のMultiIndex判定を`isinstance(...,pd.MultiIndex)`に統一(両者pandas import追加)。
- **予想精度の改善(signals.judge再設計・実証済)**: バックテスト107銘柄×3期間＋自分で30銘柄独立検証。
  「RSI売られすぎ(<30)だけが明確な予測力(10日先エッジ+2.6%)、SMA順張り/MACDはエッジ負〜ゼロ」を確認。
  → judgeを **RSI<30:+3 / RSI<40:+1 / RSI>70:-2、トレンドSMA25>75:±1、5×25クロス:±1、MACDは買いから除外** に再設計。
  「買い」=実質『上昇トレンド中の売られすぎ＝押し目』のみ点灯(下降中のRSI<30=score+2=様子見=落ちるナイフ回避)。
  実測: 新judge買い日の10日先 **勝率54%→64.9%・エッジ+2.34%**(245件/30銘柄6年)。回帰テスト36/36維持。
- buy&holdの総リターンには依然届かない(本ユニバースでは不可避)。改善の主眼は「予想の的中率」と「下降相場での回避」。

**✅(2026-06-27)第24陣: scanner(通知の買い候補)も実証改善**
- scannerの2タイプをタイプ別に前方リターン実測(30-40銘柄6年):
  ・**押し目**(上昇中×RSI≤42): エッジ+0.4〜1.2%・勝率60〜64%=元から良い→維持。
  ・**急騰ブレイク**(20日高値×出来高1.3x): エッジ+0.1〜0.5%・勝率52〜57%=ほぼ無価値＋3倍頻発でノイズ。
- 絞り込み案を比較→**「RSI<65」追加が決定的**(買われすぎブレイク=高値掴みを除外)。40銘柄×5/10/20日で頑健:
  10日先エッジ+0.2%→**+1.9%**・勝率56%→**61%**・件数1937→377(ノイズ5分の1)。vol1.8x/ret20制限は効かず不採用。
- 実装: `scanner.analyze_one`の急騰ブレイク条件に `and rsi < 65` を1つ追加。
- 確認: 全銘柄scanで急騰ブレイクは全てRSI<65。回帰テスト36/36。実験scriptはscratchpad(verify_scanner/breakout/b2.py)。

**✅(2026-06-28)第25陣: 売り/リスク側の実証→過剰最適化せず文言を是正**
- **売り(天井)予測は本ユニバースで優位性なし**と実証(30銘柄): 新judge「売り」後の10日先は+1.45%(むしろ上昇)・下落率45.7%、
  RSI>70はエッジ-0.18%。→無理に売りシグナルを足さない(教訓「暴落予測の魔法はない」と一致)。売りは損切り/利確ライン＋暴落検知に委ねる。
- **risk.pyの「要注意」は“さらに下がる”ではなく“短期反発しやすい”と判明**(40銘柄): 急落-5%→10日先+3.37%(勝率61%)、
  新安値→+1.95%、ギャップ-3%→+3.19%。急な下げ＝売られすぎの反発が多い(RSI<30エッジ+2.6%と整合)。
- 対応: 過剰最適化を避け**コードのシグナルは変えず**、notify_jobのリスク文言だけ是正:
  「急変・大きな下げ(要チェック)＋『急な下げは短期反発しやすい。慌てて売らず損切りラインで判断』」。底でのパニック売り防止。
  ※真の暴落では下げ続けるので損切りは守る、と明記。ダッシュボードはdetect_risks未表示なので変更不要。
- 結論(今セッションの予想改善): 買い=押し目/伸びしろブレイクに精度向上(勝率↑)、売り=予測不能と認め機械的ルールに委ねる、という実証ベースの設計に。

**✅(2026-06-28)第26陣: universe点検→死銘柄削除**
- 全108銘柄を取得監査: 重複なし・低流動性ゼロ(全て流動性十分な大型株=良質)。
- 唯一 **9613.T NTTデータ が上場廃止**(2025年NTTのTOBで非公開化)→毎回のscan/risk/notifyでエラーを出していた。削除(→107銘柄)。
- イオン8267は残存。scan全銘柄完走・9613エラー消滅を確認。同IT/SI成長株を足したい場合はuniverse.pyに追記可。
- 監査script: scratchpad/audit_universe.py。

**✅(2026-06-30)第27陣: 📌見張り(実保有の利確/損切りアラート)**
- 背景: 楽天かぶミニ(単元未満株)は逆指値=自動損切りができない(指値は2024/8〜対応だが±1%・当日中)。実保有はアラートで補う。
- 新モジュール `src/holdings.py`: store `hold:<user>`={code:{name,target,stop}}。set_line/remove/load。
- `notify_job.build_holding_alerts()`: hold:* 利用者を走査し利確/損切りラインを直近終値と比較→到達/接近(NEAR_ALERT_PCT)を通知。
  dedupは `holdsent:<user>`。`build_paper_alerts`と同じ_level_status/RANK方式。main()で**ペーパー＋実保有をまとめて最優先の別pushに**。
- ダッシュボードに「📌 見張り」ページ追加(PAGESに挿入): 銘柄選択＋利確/損切り入力で登録、見張り中リスト(現在値＋各ラインまでの距離)＋外すボタン。
- 検証: 実保有アラート到達通知→2回目0(dedup)、見張りページAppTest OK、回帰テスト36/36。短期コード併記も反映済。
- 補足: コード表示(short_code)機能(第26.5)＝🔎候補/通知に［8267］［AAPL］併記、今ここ!にお気に入りトグルボタン追加も同セッションで実装済。

**✅(2026-06-30)第28陣: トレーリングストップ/お気に入りタブ/見張り登録ボタン/検索/ロボ株/バグ修正**
- トレーリングストップ(holdings trail+peak、build_holding_alertsで動的売りライン、📌見張りに%入力)。
- ⭐お気に入り一覧ページ(状態＋分析/見張り/解除ショートカット)。🔎今ここ!に「📌見張りに登録(損切/利確付き)」ボタン。
- 銘柄を名前/番号で検索(fmt_stock/SEARCH_HINT、主要ピッカー)。universeに人型ロボ部品5銘柄(6324/6506/6268/6481/6723)追加→112銘柄。
- **エージェント監査でバグ2件発見→修正**:
  ①[中] 📌見張りフォームの数値入力が固定キー+value=でStreamlitが既存値を無視→銘柄切替で前の値(0)が残り、保存で
    その銘柄が削除される事故。→入力キーを銘柄ごと(`hold_tgt_{hcode}`等)に変更で解決。
  ②[低] 今ここ!の見張り登録ボタンがtrail既定0で既存トレーリングを消す。→`set_line`のtrailを`None`(未指定=既存維持)に変更。
  検証: trail維持/解除/削除の各分岐＋見張りページAppTest＋回帰36/36 OK。
- 回帰スイープ(構文0/テスト36/全9ページ0NG)もクリア。エージェントの「問題なし」項目: dedup/peak保存/利用者分離/_goto遷移/key重複/ゼロ除算ガード。
- **財務データ拡充(フル装備)**: 新規`fundamentals.py`。
  ①`extra_metrics(info)`=ROE/ROA/各利益率/配当性向/D-E/流動比率/CF/BPS/PEG(.infoから・追加通信なし)。
  ②`financial_summary(code)`=BS/PLの最大5期(総資産/総負債/自己資本/自己資本比率/売上/営業利益/純利益)。ラベル表記ゆれ・None耐性。
  📊銘柄分析: ファンダ枠にROE/自己資本比率/営業利益率/配当性向を追加(自己資本比率40%↑緑/20%↓赤)＋「📑財務(5期の推移)」エクスパンダー(dataframe)。get_financials=30分キャッシュ。
  🤖AI分析: gather_inputs/build_promptに「財務健全性・収益性」セクション追加、出力に`financial_take`(🏦欄)追加。
  検証: 実データ(6920 ROE44%/自己資本比率41→64%)＋None安全＋回帰36/36＋銘柄分析AppTestスモークOK。
- **財務"全部盛り"追加**: extra_metricsにpsr/ev_ebitda/ebitda_margin/op_cf_margin(営業CF÷売上)/total_revenue追加。
  📑財務エクスパンダーを「詳細指標・5期の推移」に拡張し、割安度(PSR/EV-EBITDA/PEG/予想PER)・収益性(ROA/純利益率/EBITDA率/営業CFマージン)・
  安全性(D-E/流動比率/当座比率/BPS)・キャッシュ(営業CF/FCF/現金/有利子負債)の4ブロックを表示。
  AI分析にもPSR/EV-EBITDA/D-E/EBITDA率/営業CFマージンを追加。回帰36/36・AppTestスモークOK。
- **💬AI相談のバグ修正+財務対応**: `get_my_portfolio`ツールが定義だけで実装なし(呼ぶとunknown toolエラー)
  →`_tool_portfolio`実装(保有/現値/含み損益/利確損切ライン/現金)。respond/run_toolに`user=`を通しdashboardからUSERを渡す。
  get_stockは`gather_inputs`経由なので財務(ROE/自己資本比率推移/PSR等)は自動で入る。ツールdescriptionを財務対応に更新。
- **買う前の安全チェック"全部盛り"**: 新規`checks.py`(next_earnings=未来日のみ採用/liquidity_20d=平均売買代金/
  drawdown=52週高値比/volatility/sector_jp/precheck=警告リスト/portfolio_sectors=セクター集中)。
  📊銘柄分析に「⏱️リスク・タイミング」カード(次回決算・52週高値からの下落・ベータ・売買代金、警告つき)。
  💰ペーパー: 買うボタン前に「✅買う前チェック」(決算7日以内⚠️/流動性1億円未満⚠️/DD30%超⚠️/損切り未設定⚠️/総資産30%超集中⚠️)、
  保有2銘柄以上でセクター構成表示(50%超で⚠️)。AI分析/AI相談にも次回決算・流動性・DD・ベータ・セクターを材料追加。
  ※信用倍率はyfinance日本株で取得不可(shortRatio=None)のため見送り。業績予想修正はtdnetのIMPORTANT判定で既カバー。
  検証: None安全・実データ(7203 決算8/6・35日後)・回帰36/36・銘柄分析+ペーパーAppTestスモークOK。

## 未確認/小タスク
- ~~クラウドからUpstashへ実書き込み~~ **✅確認済**: 手元から`store`でUpstashへ書込/読取/削除が通り、
  クラウド作成の`paper:ken`も読めた=永続化は機能。（残: クラウドUI上での⭐操作の体感確認だけ）
- ~~`use_container_width=True` → `width='stretch'`~~ **✅置換完了**(dashboard.py 10箇所・st1.58で対応)。
- デスクトップの一時ファイルは削除済。

## 到達アラートの仕組み(メモ)
- 新規: `store.list_keys(pattern)`(Upstashは KEYS / ファイルはdata/kv列挙)、`paper.all_users()`、
  `paper`の`_new_state`に`alerts_sent`追加、`notify_job._fetch_prices`/`build_paper_alerts`。
- targets/stopsは絶対株価(円)。利確=現値≥target、損切り=現値≤stop。
- 通知ONOFFは無し(課金なし=ローカル価格取得のみ)。AIコメントとは別系統。

## 参考
- `claude-code-main/`（中島聡氏のClaude Codeソース・投資アプリではない）gitignore済。学び=ツール使用ループ(💬AI相談に採用)。他に動的システムプロンプト等が応用可。
- 全体像は README.md。
