# 再開メモ（次のセッションはここから）

最終更新: 2026-06-24

## いまの状態（できたこと）
- ダッシュボード（ブラウザ画面・スマホ対応）完成: `src/dashboard.py` ＋ `dashboard.bat`
  - 3タブ: 🔎今ここ！(スキャナー) / 📊銘柄分析(チャート＋ファンダ) / 📰ニュース
- 全銘柄スキャナー完成・動作確認OK: `src/scanner.py`（108銘柄→押し目/急騰ブレイクを検出）
- スマホ通知(ntfy)動作確認OK: `src/notify.py`（トピック tousi-knak-r7m3q9）
- streamlit / plotly インストール済み
- 設定: `src/config.py`（損切り5%/利確10%、SCAN_TOP_N=8）

## ローカルで今すぐ見る方法
- `dashboard.bat` をダブルクリック → ブラウザでダッシュボードが開く
  - 初回「Welcome to Streamlit / Email」が出たら、空のままEnter（メール不要）

## 次にやること（クラウド化＝PC不要にする）
ゴール: PCの電源を切っても、スマホでどこでも使える状態。
1. git / gh が入っているか確認（未確認）。無ければインストール案内。
2. プロジェクトをGitHubにpush（ユーザーはGitHubアカウント有り・未使用）。private推奨。
   - 用意するファイル: requirements.txt, .gitignore
3. Streamlit Cloud に接続してダッシュボードを公開（PC不要で24時間）。
4. GitHub Actions(cron)で朝夜の通知を自動化（PC不要）。
   - 朝8:15 JST=前日23:15 UTC / 夜18:30 JST=09:30 UTC
   - 通知用スクリプト notify_job.py を作る（scan→ntfy送信）

## 未対応の依頼
- ダッシュボードの用語・マークを「タップで説明が出る」ようにする（ツールチップ help= ＋ 用語ガイドの追加）。

## 注意
- 前セッションはツール操作の不具合で画面が乱れた。新セッションで継続すればOK。
- 詳しい全体像は同フォルダの README.md を参照。
