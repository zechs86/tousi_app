# Python仮想環境(各PCで作り直すのでアップしない)
.venv/
__pycache__/
*.pyc

# 蓄積データ(大きい・自動再生成できるのでアップしない)
data/*.db
data/*.sqlite
data/*.json

# OS/エディタの一時ファイル
.DS_Store
Thumbs.db
.streamlit/secrets.toml

# 手元だけの秘密(通知の合言葉など)。公開しない。
src/secret_local.py

# 参考用に置いた外部コード(中島聡さんのclaude-code-main)。手元で見るだけ・リポジトリには含めない。
claude-code-main/
